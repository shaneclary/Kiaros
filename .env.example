# Kiaros 2.0 — Environment Variables (all optional)
# Copy to .env and modify as needed

# Server port (default: 3333)
# PORT=3333

# Note: API keys should NEVER be placed here.
# They are managed through the Kiaros UI and stored encrypted.
